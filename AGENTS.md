# Agent Notes

- Keep the site content-driven: routes come from `content/<track>/<group>/<page>.md`.
- Use TypeScript for all code fences: ` ```ts `.
- Keep lesson pages concise: signals, template, pitfalls.
- Do not add decorative UI or marketing copy beyond the homepage.
- Run `bun run typecheck` and `bun run build` before finishing code changes.
- Avoid emojis in `README.md`; content pages may keep the existing emoji field style.
